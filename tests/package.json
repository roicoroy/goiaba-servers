{
  "name": "medusa-strapi-tests",
  "version": "1.0.0",
  "description": "Integration tests for Medusa and Strapi unified auth system",
  "scripts": {
    "test": "jest",
    "test:medusa": "jest medusa-health.test.js",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "axios": "^1.6.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 30000,
    "verbose": true,
    "collectCoverageFrom": [
      "**/*.{js,ts}",
      "!**/node_modules/**"
    ]
  }
}